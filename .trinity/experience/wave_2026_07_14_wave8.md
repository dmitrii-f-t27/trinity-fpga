# WAVE 8 REPORT — 2026-07-14

## Executive Summary

Deepest audit yet: found paper's central quantitative claim ("71/83 formats") was a double-count, "-nodsp mandatory" was false, GF64 "70.1%" was non-reproducible from HEAD (clamp regressed it). All fixed. Clamp reverted. Paper honest. CI flags consistent.

---

## CRITICAL FIXES (S1-S4)

### S1: "71/83 formats" → "~41/83" ✅
Paper double-counted: 41 decode cells + 16 compute cells = "71 formats." But the 16 compute cells are for formats that ALREADY have decode cells. Honest count: ~41 unique formats with silicon cells.

Fixed in 4 locations: abstract, intro, §4.1, conclusion.

### S2: "-nodsp mandatory" → actual flags ✅
Paper claimed every design uses `-nodsp`. Reality: only MUL workflows use it. ADD workflows don't. The LUT measurements were made WITHOUT -nodsp. GF16 MUL explicitly uses 1 DSP.

Fixed in 3 locations: §3.1, abstract, §6.

### S3: Clamp REVERTED ✅
The clamp (Wave 4) was committed but regressed GF64 from 70.1% → 48.9%. Paper reported 70.1% (pre-clamp). Building HEAD gave 48.9% (with clamp). Non-reproducible.

Reverted clamp from gf_adder_param.v. HEAD now reproduces 70.1%. iverilog: 9/9 ALL_PASS.

### S4: build-matrix.yml fixed ✅
- Added `-abc9` (was missing — Wave 3 proved removal causes 70→19% regression)
- Added `-flatten` (consistency with per-design workflows)
- Fixed dead if/else (both branches were identical)
- MUL gets `-nodsp`, ADD doesn't (matching reality)

---

## SKILL.md Rebuilt ✅
20 honest lessons, all contradictions fixed. Key corrections:
- "75/83 SW-bitexact" → "~62-69/83" (matches CATALOG_MATRIX)
- "decode-HW ~47" → "41 formats" (matches paper)
- "clamp applied" → "clamp REVERTED (regressed)"
- "11392/11392" retained (iverilog+silicon verified, though artifact not committed)

---

## THREE OPTIONS FOR NEXT WAVE

### Option A: "2-Stage Pipeline" — Definitive GF64 Timing Fix
The ONLY remaining path to GF64 bit-exact. Split combinational path into 2 clock stages.

**Risk**: Medium (handshake modification)
**Impact**: GF64/128/256 Tier-E unlocked

### Option B: "TX Race Auto-Fix" — 1496 Wrappers
Mechanical script to convert shift-register TX → buffer+mux across all wrappers.

**Risk**: Low
**Impact**: Catalog-wide TX race eliminated

### Option C: "Native DIV/SQRT" — Real GF Arithmetic
Replace binary32-proxy div/sqrt with native GF RTL. Fix NBA stale output. Add conformance.

**Risk**: High
**Impact**: Honest div/sqrt in the catalog
